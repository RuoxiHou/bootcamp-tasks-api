apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tasks-api-tls
  namespace: default
spec:
  secretName: tasks-api-tls
  dnsNames:
    - ${TASKS_API_HOSTNAME}
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    size: 2048
