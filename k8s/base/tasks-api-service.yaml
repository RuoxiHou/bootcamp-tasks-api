apiVersion: v1
kind: Service

metadata:
  name: tasks-api
  namespace: default

spec:
  type: ClusterIP

  selector:
    app: tasks-api

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000